#include "main.h"

/*
* Fuctions that takes a pointer to an int as a parameter,
* and updates the value it points to 98.
*@n: parameter to take the value.
*/

void reset_to_98(int *n);
{
	if (n)
		*n = 98;
}
